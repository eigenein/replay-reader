using System;

namespace Core
{
    /// <summary>
    /// Packet property type.
    /// </summary>
    public enum PropertyType
    {
        Subtype,
        PlayerId,
        Health,
        DestroyedTrackId,
        Source,
        Target,
        Position,
        HullOrientation,
        Message,
        AltTrackState,
    }
}
