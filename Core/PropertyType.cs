using System;

namespace Core
{
    /// <summary>
    /// Packet property type.
    /// </summary>
    public enum PropertyType
    {
        PlayerId,
        Health,
        DestroyedTrackId,
        Source,
        Target,
        Position,
        HullOrientation,
        Message,
        AltTrackState,
    }
}
